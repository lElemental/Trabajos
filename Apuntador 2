
#include <stdio.h>

int main()
{
    int numero = 42;
    int *puntero = &numero;
    *puntero = 20;
    printf("Valor de la variable : %i\n",*puntero);
    printf("%i\n", *puntero);
    printf("%i\n", &puntero);
    printf("%i\n", puntero);

    return 0;
}

